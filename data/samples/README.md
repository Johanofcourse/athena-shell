# Sample data drop

Drop the manually-downloaded files here (see `docs/ROADMAP.md` Phase 3):

- A Redfin Data Center "Price Drops" export
- A Redfin Data Center "Home Delistings & Relistings" export
- One Housing Market Tracker export (any single metro is fine)

These are small reference files for schema/quality review, not the
production data pipeline - keep them if they're small (a few MB or
less), otherwise gitignore the specific file and just share column
headers + a few sample rows instead.
